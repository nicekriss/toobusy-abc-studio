# 변경 이력

## 0.3.0 — 노래 샘플 채보 및 마이크 성부 선택 개선

- 반주가 있는 노래를 로컬 HDemucs로 보컬 분리하고 pYIN으로 선율을 추출한다.
- 구간 선택, 템포 추정, 원본·보컬·멜로디 미리듣기, 명시적 반영과 취소를 추가했다.
- 조·박자 강제 보정 기본값을 제거하고 원음 타이밍을 보존한다.
- 짧은 무음과 반복음의 재시작을 보존하며 음 종료를 재시작으로 오인하지 않도록 검사한다.
- 마이크의 불안정한 음색 자동 분류를 제거하고 인스/보컬 목적지를 직접 선택한다. 없는 성부는 성공한 녹음에만 생성한다.
- 녹음 시작 위치·성부를 고정하고 연결 대기/분석 중 중복 시작과 닫기 후 뒤늦은 반영을 방지한다.
- ABC 출력의 마디 내 임시표·내추럴 상태를 수정했다.
- 새 악보 반영을 포함해 전체 악보와 BPM을 되돌릴 수 있게 했다.


## 0.2.0

YuE2 연동 노드 `2BZ YuE2 Generate + ABC` 를 넣었다. 설치된 ComfyUI-YuE2 의 생성
노드를 찾아 상속하고 `abc` 를 항상 보이는 칸으로 올린다. 그래야 악보 스튜디오
버튼이 그 칸에 붙는다. YuE2 가 없으면 그 노드만 설치 안내를 띄우고 나머지 ABC
기능은 그대로 동작한다.

연동 노드는 워커 호출부를 복사하지 않고 상위 `generate()` 에 그대로 위임한다.
복사본에는 악보와 `planning=off` 를 함께 넣었을 때 띄우는 한국어 안내가 빠져
있었고, 그 탓에 YuE2 내부 영어 예외가 그대로 올라왔다. 실제로 이 조합 하나
때문에 연동 테스트가 한참 막혔다. 오류 메시지 줄바꿈이 글자로 보이던 문제도
위임으로 함께 사라졌다.

테스트를 셋 더했다. 상위에 여섯 인자를 그대로 넘기는지, 빈 악보가 자동 작곡으로
가는지, `planning=off` 안내가 살아 있는지를 고정한다. 가짜 업스트림을 붙여
ComfyUI 없이 돈다.

## 0.1.0

toobusy 노드팩에서 ABC 악보 스튜디오를 별도 저장소로 분리했다.
toobusy v0.5.1 시점의 코드를 그대로 가져왔으므로 기능 차이는 없다.

포함된 내용:

- `abc` 텍스트 위젯을 가진 노드에 "🎼 악보 스튜디오 열기" 버튼을 붙인다
- 피아노롤 편집기 (한글 도레미 건반, 그리기·선택·지우개, 미니맵, 확대)
- 코드 반주 재생, 건반 클릭 미리듣기
- 마이크 허밍 / 음원 파일 구간을 YIN 피치 검출로 따서 멜로디로 그려 넣기
- `abc` 칸이 입력 소켓이고 선이 꽂혀 있으면 선을 따라가 근원 노드의
  텍스트 칸에 넣는다 (toobusy v0.5.1에서 고친 내용)
- 보조 노드: ABC 악보 입력, ABC 악보 정보
